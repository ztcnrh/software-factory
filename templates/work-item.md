# <title>

> A hand-written work item. Drop the gist here; the **triage station** reads this
> first and decides where it goes. You don't need to be precise — that's triage's
> job. (Equivalent to `factory new "<title>" --body "..."` or opening a GitHub
> issue with the `factory:triage` label.)

## What
One paragraph: the feature, bug, or idea.

## Why
The value, or the pain it removes.

## Notes
Links, context, constraints, anything you already know. More here = better triage.
