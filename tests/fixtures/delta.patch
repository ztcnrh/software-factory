diff --git a/src/tally/__init__.py b/src/tally/__init__.py
index 45e69d5..1379ade 100644
--- a/src/tally/__init__.py
+++ b/src/tally/__init__.py
@@ -6,9 +6,12 @@
 import csv
 import io
 import json
+import re
 import sys
 from pathlib import Path
 
+_THOUSANDS = re.compile(r"[+-]?\d{1,3}(,\d{3})*(\.\d+)?")
+
 
 class TallyError(Exception):
     """A user-facing tally failure: a message plus the exit code it maps to."""
@@ -45,7 +48,10 @@ def cell_values():
                     cell = (row.get(column) or "").strip()
                     if cell:
                         try:
-                            value = float(cell.removeprefix("$").replace(",", ""))
+                            numeral = cell.removeprefix("$")
+                            if "," in numeral and not _THOUSANDS.fullmatch(numeral):
+                                raise ValueError(numeral)
+                            value = float(numeral.replace(",", ""))
                         except ValueError as exc:
                             raise TallyError(
                                 f"tally: value {cell!r} in column {column!r} on row "
diff --git a/tests/test_total.py b/tests/test_total.py
index 595294d..52495ed 100644
--- a/tests/test_total.py
+++ b/tests/test_total.py
@@ -1,7 +1,9 @@
 import csv
 from pathlib import Path
 
-from tally import tally, total
+import pytest
+
+from tally import TallyError, tally, total
 
 
 def test_total_sums_the_named_column(tmp_path: Path):
@@ -20,6 +22,16 @@ def test_total_parses_currency_formatted_cells(tmp_path: Path):
     assert total(f, "amount") == 1246.56
 
 
+def test_total_rejects_a_malformed_comma_grouping(tmp_path: Path):
+    """A comma that is not a well-formed thousands separator must still fail loudly,
+    not silently sum to a wrong total (e.g. a European 1.234,56 decimal-comma value).
+    """
+    f = tmp_path / "d.csv"
+    f.write_text('name,amount\na,"1.234,56"\n')
+    with pytest.raises(TallyError):
+        total(f, "amount")
+
+
 def test_total_treats_a_blank_cell_as_zero(tmp_path: Path):
     """A blank cell (e.g. no charge) must not crash the sum; it contributes 0."""
     f = tmp_path / "d.csv"
